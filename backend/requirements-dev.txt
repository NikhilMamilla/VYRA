# Development and test tooling. Install on top of the runtime requirements:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
aiosqlite==0.22.1   # in-process database for the test suite
ruff==0.16.5
